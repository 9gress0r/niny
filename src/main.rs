mod tokens;
use std::fs;

fn main() {
  /*
  let content = fs::read_to_string("test.nn").expect("Should have been able to read the file");
  let tokenizer = tokens::Tokenizer::new(content);

  for token in tokenizer.tokenize() {
    println!("{}", token);
  }
  */
}
